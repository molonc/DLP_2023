function [stageOrder] = snake(rows,cols)
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here
    stageOrder = [];
    for i = 1:rows
        for j = 1:cols
            if rem(i, 2) == 0 
                stageOrder(end+1) = [i cols - j]
            else
                stageOrder(end+1) 
            end
end